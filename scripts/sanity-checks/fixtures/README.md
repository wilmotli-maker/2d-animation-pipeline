Drop a small test image here as `test-image.png` (any cheap, low-res reference
image works — it's only used to confirm the `--start-image` upload path in
sanity check 5, not for quality evaluation).

Or point `SANITY_TEST_IMAGE` at a file elsewhere:

```bash
SANITY_TEST_IMAGE=/path/to/other/image.png ./05-file-io-roundtrip.sh
```
